# 住院信息管理系统：功能与文件对应说明

## 1. 当前实现概览

本项目采用 Spring Boot + MySQL + Flutter 的前后端分离结构。

- 后端：已实现认证、角色权限、基础资料、患者、住院行政、医师临床工作和统计接口。
- 数据库：使用 Flyway 自动创建 25 张业务表，并插入角色、科室、病区、床位、药品和检查项目等基础数据。
- 演示数据：首次启动时自动生成 3 个演示账号、30 名患者及诊断、病程、体征、护理、押金、费用和结算关联记录。
- 前端：已实现 Android/Web 共用的登录、令牌存储、响应式角色工作台，以及管理员、住院处、医师的核心表单、列表和业务操作页面。
- 验证：后端 `mvnw.cmd test` 通过 7 项测试；Flutter 已完成格式化、静态检查、组件测试、Web 发布构建和 Android 调试 APK 构建。

## 2. 角色与可用功能

| 角色 | 已由后端控制的功能 | 对应模块 |
| --- | --- | --- |
| 最高管理员 `SUPER_ADMIN` | 登录、基础资料维护、用户管理、患者查询、住院业务办理、临床数据查看和维护、统计查询 | `masterdata`、`admission`、`clinical`、`statistics` |
| 住院处 `ADMISSION` | 登录、患者建档、入院登记、押金收退、转科确认、出院结算、统计查询 | `patient`、`admission`、`statistics` |
| 医师 `DOCTOR` | 登录、患者查询、过敏史、诊断、病程、护理评估、生命体征、处方、检查医嘱和报告 | `patient`、`clinical` |

说明：前端按角色隐藏不相关入口只是体验层处理；最终权限仍由 Spring Security 的 JWT 身份和控制器注解判定。

## 3. 已实现的业务功能与接口

### 3.1 认证与账户安全

| 功能 | 接口 | 主要实现文件 |
| --- | --- | --- |
| 用户名和密码登录，签发 JWT | `POST /api/v1/auth/login` | `backend/src/main/java/com/hospital/auth/AuthController.java`、`security/JwtService.java` |
| 获取当前登录用户 | `GET /api/v1/auth/me` | `auth/AuthController.java`、`security/CurrentUser.java` |
| 修改本人密码 | `PUT /api/v1/auth/password` | `auth/AuthController.java` |
| JWT 请求解析、401/403 JSON 响应 | 全部受保护接口 | `security/JwtAuthenticationFilter.java`、`security/RestSecurityHandlers.java`、`security/SecurityConfig.java` |

密码使用 BCrypt 哈希保存。JWT 只携带用户 ID、登录名和角色编码；每个请求由过滤器转换为 Spring Security 认证对象。

### 3.2 基础资料与账号管理

| 功能 | 接口 | 权限 | 主要实现文件 |
| --- | --- | --- | --- |
| 查询、新增、启停科室 | `GET/POST /api/v1/master-data/departments`、`PUT /departments/{id}/status` | 最高管理员 | `masterdata/MasterDataController.java` |
| 查询、新增病区 | `GET/POST /api/v1/master-data/wards` | 最高管理员 | `masterdata/MasterDataController.java` |
| 查询、新增、启停床位 | `GET/POST /api/v1/master-data/beds`、`PUT /beds/{id}/status` | 最高管理员 | `masterdata/MasterDataController.java` |
| 查询、新增药品 | `GET/POST /api/v1/master-data/drugs` | 最高管理员 | `masterdata/MasterDataController.java` |
| 查询、新增检查检验项目 | `GET/POST /api/v1/master-data/exam-items` | 最高管理员 | `masterdata/MasterDataController.java` |
| 查询、新增系统用户 | `GET/POST /api/v1/master-data/users` | 最高管理员 | `masterdata/MasterDataController.java` |

床位处于 `OCCUPIED` 时不能被基础资料维护直接改为其他状态，只能由转科或出院流程释放。

### 3.3 患者档案与过敏史

| 功能 | 接口 | 权限 | 主要实现文件 |
| --- | --- | --- | --- |
| 按姓名、院内编号、身份证号分页查询患者 | `GET /api/v1/patients` | 已登录 | `patient/PatientController.java` |
| 新建患者档案 | `POST /api/v1/patients` | 住院处、最高管理员 | `patient/PatientController.java` |
| 查询患者基本信息和过敏史 | `GET /api/v1/patients/{id}` | 已登录 | `patient/PatientController.java` |
| 新增患者过敏史 | `POST /api/v1/patients/{id}/allergies` | 医师、最高管理员 | `patient/PatientController.java` |

患者编号由服务端生成；过敏记录人取自当前 JWT 身份，不能由客户端伪造。

### 3.4 入院、押金、转科与出院结算

| 功能 | 接口 | 权限 | 主要实现文件 |
| --- | --- | --- | --- |
| 按状态查询住院记录 | `GET /api/v1/admissions` | 已登录 | `admission/AdmissionController.java` |
| 查询可选科室、可用床位、责任医师 | `GET /api/v1/admissions/departments`、`/available-beds`、`/doctors` | 对应住院业务角色 | `admission/AdmissionController.java` |
| 办理入院并分配床位 | `POST /api/v1/admissions` | 住院处、最高管理员 | `admission/AdmissionController.java`、`admission/AdmissionService.java` |
| 查询单次住院、押金和转科历史 | `GET /api/v1/admissions/{id}` | 已登录 | `admission/AdmissionController.java` |
| 缴纳、退款或冲正押金 | `POST /api/v1/admissions/{id}/deposits` | 住院处、最高管理员 | `admission/AdmissionController.java`、`admission/AdmissionService.java` |
| 提交转科申请 | `POST /api/v1/admissions/{id}/transfers` | 医师、住院处、最高管理员 | `admission/AdmissionController.java`、`admission/AdmissionService.java` |
| 确认转科 | `POST /api/v1/admissions/transfers/{transferId}/approve` | 住院处、最高管理员 | `admission/AdmissionController.java`、`admission/AdmissionService.java` |
| 出院结算 | `POST /api/v1/admissions/{id}/settlement` | 住院处、最高管理员 | `admission/AdmissionController.java`、`admission/AdmissionService.java` |

关键一致性规则：

- 入院时使用 `FOR UPDATE` 锁定床位，再创建住院记录并将床位改为占用。
- 押金余额由押金流水汇总；退款金额不得超过可用余额。
- 确认转科时同时释放原床位、占用目标床位、更新当前科室和床位、记录审批人。
- 出院结算时统计未结费用；押金不足会拒绝出院，余额则生成退款流水；结算、费用状态、出院状态和床位释放在同一事务中完成。

### 3.5 医师临床工作

| 功能 | 接口 | 主要实现文件 |
| --- | --- | --- |
| 查询本次住院临床概览 | `GET /api/v1/admissions/{admissionId}/clinical` | `clinical/ClinicalController.java` |
| 新增诊断 | `POST /clinical/diagnoses` | `clinical/ClinicalController.java` |
| 保存病程记录草稿 | `POST /clinical/records` | `clinical/ClinicalController.java` |
| 录入护理评估 | `POST /clinical/assessments` | `clinical/ClinicalController.java` |
| 录入生命体征 | `POST /clinical/vital-signs` | `clinical/ClinicalController.java` |
| 开立处方及药品明细 | `POST /clinical/prescriptions` | `clinical/ClinicalController.java`、`clinical/ClinicalService.java` |
| 开立检查检验医嘱 | `POST /clinical/exam-orders` | `clinical/ClinicalController.java`、`clinical/ClinicalService.java` |
| 发布检查检验报告和结果 | `POST /clinical/exam-order-items/{itemId}/report` | `clinical/ClinicalController.java`、`clinical/ClinicalService.java` |
| 查询检查单项目明细 | `GET /clinical/exam-orders/{orderId}` | `clinical/ClinicalController.java` |
| 查询医嘱可选药品和检查目录 | `GET /api/v1/clinical/catalog/drugs`、`/exam-items` | `clinical/ClinicalCatalogController.java` |

上述 `/clinical/...` 路径均以 `/api/v1/admissions/{admissionId}` 为前缀。普通医师只能维护自己负责且状态为 `IN_HOSPITAL` 的患者；最高管理员可审阅临床数据。

处方和检查医嘱会在开立时把药品或检查项目的单价写入收费表，后续修改目录价格不会改变既有费用。检查单包含多个项目时，每个项目独立记录报告状态，整张检查单自动更新为“部分报告”或“已完成”。

### 3.6 统计分析

| 功能 | 接口 | 数据来源 | 主要实现文件 |
| --- | --- | --- | --- |
| 科室床位总数、占用数和占用率 | `GET /api/v1/statistics/bed-occupancy` | `department`、`ward`、`bed` | `statistics/StatisticsController.java` |
| 主要诊断患者数和平均住院天数 | `GET /api/v1/statistics/diagnoses` | `diagnosis`、`admission` | `statistics/StatisticsController.java` |
| 药品、检查等费用构成 | `GET /api/v1/statistics/charges` | `charge` | `statistics/StatisticsController.java` |

统计对最高管理员和住院处开放，全部以业务表真实数据聚合，不使用前端模拟数据。

## 4. 文件夹与文件职责

### 4.1 项目根目录

| 文件或文件夹 | 当前作用 |
| --- | --- |
| `backend/` | Spring Boot、MySQL、Flyway 和后端测试工程。 |
| `frontend/` | Flutter Android/Web 客户端工程。 |
| `refer_pics/` | 需求分析时参考的住院业务界面图片，不参与程序运行。 |
| `数据库课程设计题目及要求-2026.pptx` | 课程任务书和验收要求的原始材料，不参与程序运行。 |
| `住院信息管理系统-实体与关联设计.md` | 实体、属性和关联关系的设计文档。 |
| `README.md` | 项目简介、MySQL 配置、后端启动方式和默认演示账号。 |
| `系统功能与文件对应说明.md` | 本文档：功能、接口和实现文件的对应关系。 |
| `.gitignore` | 版本控制时忽略构建产物、本地配置等不应提交的文件。 |

### 4.2 `backend/`：后端工程

| 文件或文件夹 | 当前作用 |
| --- | --- |
| `backend/pom.xml` | Maven 依赖与构建配置：Spring Web、Security、Validation、JPA、Flyway、MySQL、JWT 和测试框架；当前使用 Java 17。 |
| `backend/mvnw`、`backend/mvnw.cmd` | Maven Wrapper，分别供 macOS/Linux 和 Windows 执行 Maven。 |
| `backend/.mvn/wrapper/` | Maven Wrapper 的下载和版本配置文件。 |
| `backend/src/main/` | 后端正式运行的 Java 源码、配置和数据库迁移文件。 |
| `backend/src/test/` | 后端自动化测试源码。 |
| `backend/target/` | Maven 编译和测试自动生成的 `.class`、报告等文件，不是需要维护的功能源码。 |

### 4.3 `backend/src/main/java/com/hospital/`：后端 Java 源码

| 文件夹或文件 | 当前作用 |
| --- | --- |
| `HospitalApplication.java` | Spring Boot 启动入口，加载全部控制器、数据库迁移和安全组件。 |
| `admission/` | 住院行政领域：入院、押金、转科和结算。 |
| `admission/AdmissionController.java` | 定义住院相关 HTTP 接口、请求参数校验和角色权限。 |
| `admission/AdmissionService.java` | 实现床位锁定、押金余额计算、转科审批、结算退款和事务回滚规则。 |
| `auth/` | 身份认证领域。 |
| `auth/AuthController.java` | 实现登录、当前用户查询和修改密码接口。 |
| `bootstrap/` | 演示数据初始化模块。 |
| `bootstrap/DemoDataInitializer.java` | 首次启动生成演示账号、30 名患者以及住院关联数据；已有用户时不重复生成。 |
| `clinical/` | 医师临床工作领域。 |
| `clinical/ClinicalController.java` | 定义诊断、病程、护理、体征、处方、检查医嘱和报告接口。 |
| `clinical/ClinicalService.java` | 实现临床数据范围校验、处方和检查收费快照、检查报告状态推进的事务规则。 |
| `clinical/ClinicalCatalogController.java` | 向医师提供只读药品和检查检验目录，供页面以选项方式开立医嘱。 |
| `common/` | 全局响应和错误处理。 |
| `common/ApiResponse.java` | 定义统一 JSON 响应结构：`code`、`message`、`data`。 |
| `common/BusinessException.java` | 表示可预期的业务冲突，例如床位不可用、押金不足。 |
| `common/GlobalExceptionHandler.java` | 把业务冲突、参数错误、权限错误和未知错误转换为统一 HTTP/JSON 响应。 |
| `masterdata/` | 系统基础资料领域。 |
| `masterdata/MasterDataController.java` | 管理员维护科室、病区、床位、药品、检查项目和系统用户。 |
| `patient/` | 患者主档案领域。 |
| `patient/PatientController.java` | 实现患者分页查询、建档、详情和过敏史维护。 |
| `security/` | JWT 认证与 Spring Security 配置。 |
| `security/SecurityConfig.java` | 配置无状态安全链、跨域、公开接口、JWT 过滤器顺序和 BCrypt。 |
| `security/JwtService.java` | 签发、解析和校验 JWT 的签名、过期时间和声明。 |
| `security/JwtAuthenticationFilter.java` | 从 `Authorization: Bearer ...` 请求头读取令牌并写入安全上下文。 |
| `security/JwtUser.java` | JWT 中保存的最小认证主体：用户 ID、登录名、角色编码。 |
| `security/CurrentUser.java` | 从当前请求安全上下文取得经过过滤器认证的用户。 |
| `security/RestSecurityHandlers.java` | 将未认证和越权情况统一输出为 401/403 JSON。 |
| `statistics/` | 住院业务统计领域。 |
| `statistics/StatisticsController.java` | 提供床位、诊断住院天数和费用构成统计接口。 |

### 4.4 `backend/src/main/resources/`：运行配置与数据库

| 文件或文件夹 | 当前作用 |
| --- | --- |
| `application.yml` | MySQL 地址和账号环境变量、Flyway 路径、端口、JWT 密钥与有效期、CORS 允许来源配置。 |
| `db/migration/` | Flyway 数据库版本迁移目录，启动时按版本顺序执行。 |
| `db/migration/V1__core_schema.sql` | 创建核心表、外键、唯一约束和索引。包括角色、科室、用户、病区、床位、患者、过敏史、药品、检查项目、住院、转科、诊断、病程、处方、检查报告、护理、体征、费用、押金和结算表。 |
| `db/migration/V2__reference_data.sql` | 写入三类角色及演示所需的科室、病区、床位、药品、检查检验基础目录。 |

### 4.5 `backend/src/test/`：后端测试

| 文件或文件夹 | 当前作用 |
| --- | --- |
| `src/test/java/com/hospital/security/JwtServiceTest.java` | 验证 JWT 能正确签发并解析出用户 ID、登录名和角色。 |
| `src/test/java/com/hospital/workflow/WorkflowServiceTest.java` | 验证床位冲突、押金超额退款、目标床位不可用、押金不足结算、停用药品和取消项目报告等拒绝路径。 |

### 4.6 `frontend/`：Flutter 客户端

| 文件或文件夹 | 当前作用 |
| --- | --- |
| `pubspec.yaml` | Flutter 项目名称、Dart 版本和依赖：Dio、Riverpod、GoRouter、安全存储等。 |
| `analysis_options.yaml` | Flutter/Dart 静态检查规则，使用 `flutter_lints`。 |
| `README.md` | Flutter SDK 安装后的初始化、Web/Android 运行说明和真机地址提示。 |
| `lib/` | Flutter 业务源码目录。 |
| `web/` | Flutter Web 的 HTML 入口目录。 |
| `web/index.html` | Web 端页面元信息和 Flutter 启动脚本入口。 |
| `android/` | Android 原生平台工程、Manifest、Gradle 构建配置和 Kotlin 启动类。 |

### 4.7 `frontend/lib/`：Flutter 源码

| 文件夹或文件 | 当前作用 |
| --- | --- |
| `main.dart` | Flutter 应用入口，创建 Riverpod 根作用域、登录路由和工作台路由，配置 Material 主题。 |
| `core/` | 与具体页面无关的通用能力。 |
| `core/api_client.dart` | 使用 Dio 调用后端；支持 `API_BASE_URL` 覆盖地址；保存和自动附加 JWT；处理 401、统一业务错误以及 GET/POST/PUT 请求。 |
| `core/ui_helpers.dart` | 提供通用表单弹窗、JSON 表格、加载状态和中文错误提示组件。 |
| `models/` | 前端业务数据模型。 |
| `models/current_user.dart` | 表示登录响应中的当前用户 ID、用户名、角色和真实姓名。 |
| `features/` | 按功能划分的页面和状态管理。 |
| `features/auth/` | 登录和会话状态功能。 |
| `features/auth/auth_controller.dart` | 维护全局 API 客户端、当前用户状态、登录和退出操作。 |
| `features/auth/login_screen.dart` | 登录表单、参数校验、请求加载状态、错误提示和登录后的页面跳转。 |
| `features/home/` | 登录后的角色工作台。 |
| `features/home/dashboard_screen.dart` | 根据角色装配对应工作台；Web 使用侧栏，Android 使用底部导航，并支持令牌失效后返回登录页。 |
| `features/admin/admin_workspace.dart` | 管理员的科室、病区、床位、药品、检查项目、用户维护和三类统计页面。 |
| `features/admission/admission_workspace.dart` | 住院处的患者建档查询、入院分床、押金收退、转科申请确认和出院结算页面。 |
| `features/clinical/clinical_workspace.dart` | 医师的患者选择、临床概览、诊断、病程、护理、体征、处方、检查医嘱和报告页面。 |

## 5. 数据库表与功能归属

| 表类别 | 表名 | 对应功能模块 |
| --- | --- | --- |
| 权限与组织 | `role`、`system_user`、`department`、`ward`、`bed` | 认证、账号管理、基础资料、床位分配 |
| 患者主档 | `patient`、`patient_allergy` | 患者建档、患者查询、过敏史 |
| 医嘱目录 | `drug`、`exam_item` | 药品维护、检查检验项目维护、处方、检查医嘱 |
| 住院行政 | `admission`、`department_transfer` | 入院、转科、出院 |
| 临床记录 | `diagnosis`、`medical_record`、`nursing_assessment`、`vital_sign` | 诊断、病程、护理、体征 |
| 处方与报告 | `prescription`、`prescription_item`、`exam_order`、`exam_order_item`、`exam_report`、`exam_result` | 处方、检查检验医嘱、报告结果 |
| 费用结算 | `charge`、`deposit_transaction`、`discharge_settlement` | 费用快照、押金、退款、出院结算、费用统计 |

## 6. 当前未完成或需继续联调的内容

当前版本已经具备课程演示所需的三角色核心闭环。后续提升项如下：

1. 增加编辑、删除、文书签名和更细粒度的床位状态维护接口及页面。
2. 为患者、住院、临床列表补充分页、筛选器和详情页中的完整历史展示。
3. 使用独立的 MySQL 测试库执行 Flyway 集成测试和完整主流程联调。
4. 增加发布签名配置，产出可用于真实设备安装的 Release APK。
