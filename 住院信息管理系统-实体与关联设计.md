# 住院信息管理系统：实体与关联设计

> 版本：v0.1（概念设计阶段）  
> 设计依据：`数据库课程设计题目及要求-2026.pptx`、`refer_pics` 中的住院医生工作站截图，以及课程设计要求。  
> 目标：覆盖病人住院、住院押金、处方与检查检验开立、转科、出院、异常处理、基本数据维护和查询，同时保留必要的病历与护理信息。

## 1. 系统边界与用户

本系统只处理住院期间的患者、床位、医嘱、检查检验、护理记录和费用，不实现门诊挂号、药房库存、手术排班、医保实时结算等外部系统功能。药品和检查项目在本系统中作为基础目录维护，实际库存和医保接口可作为后续扩展。

系统用户采用统一的 `系统用户` 实体，当前有三种岗位：

| 岗位 | 主要职责 |
| --- | --- |
| 最高管理员 | 用户与科室维护、药品/检查项目维护、全院查询和异常处理；可执行必要的住院行政操作 |
| 住院处 | 入院登记、床位分配、押金收退、转科确认、出院登记与结算 |
| 医师 | 接诊患者、维护诊断和病历、开立处方/检查检验医嘱、录入报告结果、提交转科或出院申请 |

若最终报告必须严格限定为两类用户，可将“住院处”岗位并入最高管理员，实体和业务记录不需要改变。

## 2. 实体总览

实体按用途分为五类：

| 类型 | 实体 |
| --- | --- |
| 权限与基础资料 | 角色、系统用户、科室、病区、床位、药品、检查检验项目 |
| 患者主数据 | 患者、患者过敏史 |
| 住院业务 | 住院记录、转科记录、诊断记录、病历记录 |
| 医嘱与临床结果 | 处方、处方明细、检查检验医嘱、检查检验报告、报告结果明细 |
| 护理与费用 | 护理评估、生命体征记录、费用记录、押金流水、出院结算 |

主表使用代理主键（`*_id`）；业务编号如住院号、病案号、处方号保留唯一约束，便于查询和打印。重要业务表均建议带 `created_at`、`updated_at`，并保存操作用户。

## 3. 实体及关键属性

### 3.1 权限与基础资料

#### 3.1.1 角色（`role`）

角色是权限的稳定字典，不把角色名称直接散落在业务表中。

| 属性 | 说明 | 约束 |
| --- | --- | --- |
| `role_id` | 角色主键 | PK |
| `role_code` | `SUPER_ADMIN`、`ADMISSION`、`DOCTOR` | NOT NULL、UNIQUE |
| `role_name` | 最高管理员、住院处、医师 | NOT NULL |
| `description` | 岗位说明 | 可空 |
| `is_active` | 是否启用 | NOT NULL |

#### 3.1.2 系统用户（`system_user`）

系统登录账号与医师业务身份统一在此维护。

| 属性 | 说明 | 约束 |
| --- | --- | --- |
| `user_id` | 用户主键 | PK |
| `role_id` | 岗位 | FK -> `role.role_id` |
| `department_id` | 所属科室；住院处或管理员可为空 | FK -> `department.department_id` |
| `username` | 登录名 | NOT NULL、UNIQUE |
| `password_hash` | 密码哈希 | NOT NULL |
| `real_name` | 姓名 | NOT NULL |
| `employee_no` | 工号 | UNIQUE，可空 |
| `license_no` | 医师执业证号，非医师为空 | UNIQUE，可空 |
| `phone` | 联系电话 | 可空 |
| `status` | `ACTIVE` / `DISABLED` | NOT NULL |

#### 3.1.3 科室（`department`）

| 属性 | 说明 | 约束 |
| --- | --- | --- |
| `department_id` | 科室主键 | PK |
| `department_code` | 科室编码 | NOT NULL、UNIQUE |
| `department_name` | 如神经重症、内科 | NOT NULL、UNIQUE |
| `department_type` | 临床、医技、住院处、管理 | NOT NULL |
| `phone` | 科室电话 | 可空 |
| `status` | 启用状态 | NOT NULL |

#### 3.1.4 病区（`ward`）与床位（`bed`）

病区用于体现截图中的“科室/病区”，床位用于体现床号和占用状态。一个病区属于一个科室，一个病区有多张床。

`ward`：`ward_id`（PK）、`department_id`（FK）、`ward_code`（UNIQUE）、`ward_name`、`floor_no`、`status`。

`bed`：`bed_id`（PK）、`ward_id`（FK）、`bed_no`（同一病区内 UNIQUE）、`bed_type`（普通/重症等）、`nursing_level`（一级/二级/三级）、`status`（空闲/占用/停用）。

#### 3.1.5 药品（`drug`）

药品目录对应截图中的药名、规格、剂型、国家医保编码和价格。

`drug_id`（PK）、`drug_code`（UNIQUE）、`drug_name`、`generic_name`、`specification`、`dosage_form`、`unit`、`医保编码`（可空）、`unit_price`（`DECIMAL(10,2)`）、`stock_qty`（课程项目可选）、`status`。

#### 3.1.6 检查检验项目（`exam_item`）

统一存储检查和检验目录，用 `item_type` 区分两者，避免为相同字段建立两套表。

`exam_item_id`（PK）、`item_code`（UNIQUE）、`item_name`、`item_type`（检查/检验）、`department_id`（执行科室 FK）、`unit`（检验结果单位，可空）、`reference_range`（参考范围，可空）、`unit_price`、`status`。

### 3.2 患者主数据

#### 3.2.1 患者（`patient`）

患者是跨多次住院可复用的主数据；截图中的姓名、性别、年龄、身高体重、联系方式、证件号、民族、婚姻状况和地址均可由此支持。

`patient_id`（PK）、`patient_no`（院内患者 ID，UNIQUE）、`name`、`id_card_no`（UNIQUE，可空）、`gender`、`birth_date`、`height_cm`、`weight_kg`、`phone`、`民族`、`婚姻状况`、`出生地`、`address`、`emergency_contact_name`、`emergency_contact_phone`、`status`、`created_at`。

年龄不作为持久化字段，按 `birth_date` 和业务日期计算，避免数据过期。

#### 3.2.2 患者过敏史（`patient_allergy`）

一名患者可以有多条过敏记录，对应截图中的“过敏史录入框”。

`allergy_id`（PK）、`patient_id`（FK）、`allergen_name`、`allergy_type`（药品/食物/其他）、`result`（阳性/阴性/未发现/不详/否认）、`reaction`、`severity`、`recorded_by`（FK -> 用户）、`recorded_at`、`remark`。

### 3.3 住院业务

#### 3.3.1 住院记录（`admission`）

一次患者住院对应一条记录；同一患者可以多次住院。截图中的住院号、病案号、入院日期、床号、住院天数、护理等级、费别和医保类型均由此支持。

`admission_id`（PK）、`patient_id`（FK）、`inpatient_no`（住院号，UNIQUE）、`medical_record_no`（病案号，UNIQUE）、`admitting_department_id`（入院科室 FK）、`current_department_id`（当前科室 FK）、`attending_doctor_id`（主管医师 FK -> 用户）、`current_bed_id`（当前床位 FK）、`admission_time`、`discharge_time`（可空）、`nursing_level`、`fee_type`、`insurance_type`、`prepayment_total`（可由押金流水汇总，也可缓存）、`status`（待入院/住院中/待出院/已出院/取消）、`created_by`。

#### 3.3.2 转科记录（`department_transfer`）

转科是住院记录与科室之间的带状态历史关系，不能直接覆盖当前科室。

`transfer_id`（PK）、`admission_id`（FK）、`from_department_id`、`to_department_id`、`from_bed_id`、`to_bed_id`、`requested_by`（医师/住院处用户 FK）、`approved_by`、`request_time`、`effective_time`、`reason`、`status`（申请/已确认/已拒绝/已取消）。

#### 3.3.3 诊断记录（`diagnosis`）

一条住院记录可有多个初步、主要、并发症或出院诊断。

`diagnosis_id`（PK）、`admission_id`（FK）、`diagnosis_code`（可空）、`diagnosis_name`、`diagnosis_type`（入院/主要/并发症/出院）、`is_primary`、`diagnosed_by`（医师 FK）、`diagnosed_at`、`status`。

#### 3.3.4 病历记录（`medical_record`）

采用通用记录表承载首次病程、日常病程、转科记录、阶段小结、主治查房和出院小结等模板，保留截图中“病历书”和“病程上报史”的核心信息。

`record_id`（PK）、`admission_id`（FK）、`record_type`、`title`、`content`、`recorded_by`（医师 FK）、`recorded_at`、`signed_at`（可空）、`status`（草稿/已提交/已签名/已作废）、`version_no`。

### 3.4 医嘱与临床结果

#### 3.4.1 处方（`prescription`）与处方明细（`prescription_item`）

处方是一次开立操作的表头，药品是一对多明细。这样可以保存同一处方的开立人、状态和多种药品，而不会把药品字段重复在住院记录中。

`prescription`：`prescription_id`（PK）、`prescription_no`（UNIQUE）、`admission_id`（FK）、`doctor_id`（FK）、`prescription_type`（长期/临时/出院带药）、`ordered_at`、`start_time`、`end_time`、`status`（草稿/已提交/已审核/执行中/已停止/已作废）、`remark`。

`prescription_item`：`prescription_item_id`（PK）、`prescription_id`（FK）、`drug_id`（FK）、`dose`、`dose_unit`、`route`（口服/静脉等）、`frequency`、`days`、`quantity`、`unit_price_snapshot`、`instruction`。

#### 3.4.2 检查检验医嘱（`exam_order`）

一次医嘱可开立多个检查检验项目；每个项目单独跟踪执行和报告状态。

`exam_order`：`exam_order_id`（PK）、`order_no`（UNIQUE）、`admission_id`（FK）、`doctor_id`（FK）、`ordered_at`、`priority`、`status`（已开立/已执行/部分报告/已完成/已取消）、`clinical_note`。

`exam_order_item`：`exam_order_item_id`（PK）、`exam_order_id`（FK）、`exam_item_id`（FK）、`execution_department_id`（FK）、`scheduled_at`、`executed_at`、`status`。

#### 3.4.3 检查检验报告（`exam_report`）与结果明细（`exam_result`）

报告对应截图中的报告日期、报告名称、定量/定性结果、单位、参考范围和异常提示。

`exam_report`：`report_id`（PK）、`exam_order_item_id`（FK，原则上一对一）、`report_name`、`reported_at`、`reported_by`（可用医师/医技用户）、`conclusion`、`status`（草稿/已审核/已发布）。

`exam_result`：`result_id`（PK）、`report_id`（FK）、`item_name`、`qualitative_value`（可空）、`quantitative_value`（可空）、`unit`、`reference_range`、`abnormal_flag`、`remark`、`sort_no`。

### 3.5 护理与费用

#### 3.5.1 护理评估（`nursing_assessment`）

将截图中的入院护理评估、跌倒风险、Braden 压疮风险、生活自理能力和营养风险统一为评估记录；详细量表题目不拆成大量实体，课程项目用结构化评分字段即可。

`assessment_id`（PK）、`admission_id`（FK）、`assessment_type`、`score`、`risk_level`、`measures`、`assessed_by`（系统用户 FK）、`assessed_at`、`status`、`remark`。

#### 3.5.2 生命体征记录（`vital_sign`）

`vital_sign_id`（PK）、`admission_id`（FK）、`measured_at`、`temperature`、`pulse`、`respiratory_rate`、`systolic_bp`、`diastolic_bp`、`spo2`、`measured_by`、`remark`。

#### 3.5.3 费用记录（`charge`）

费用记录是处方、检查检验和床位等业务产生的应收费用明细，金额应在产生时保存快照，不能依赖后来变动的目录价格。

`charge_id`（PK）、`admission_id`（FK）、`source_type`（药品/检查/床位/其他）、`source_id`（来源业务记录，可空）、`item_name_snapshot`、`quantity`、`unit_price`、`amount`、`occurred_at`、`status`（未结算/已结算/已冲销）、`created_by`。

#### 3.5.4 押金流水（`deposit_transaction`）

押金只记录每笔收款、退款或冲正，余额由流水求和得到，避免并发下直接修改一个余额字段。

`deposit_txn_id`（PK）、`admission_id`（FK）、`txn_type`（缴纳/退款/冲正）、`amount`（正数）、`payment_method`、`receipt_no`（UNIQUE）、`txn_time`、`operator_id`（住院处/管理员 FK）、`remark`。

#### 3.5.5 出院结算（`discharge_settlement`）

一次住院最多一条最终结算记录；出院前应完成费用核对和押金处理。

`settlement_id`（PK）、`admission_id`（FK、UNIQUE）、`total_charge`、`deposit_used`、`refund_amount`、`additional_payment`、`settled_at`、`settled_by`、`status`（待结算/已结算/已撤销）、`remark`。

## 4. 实体关联与基数

| 关系 | 基数 | 说明 |
| --- | --- | --- |
| 角色 - 系统用户 | 1:N | 一个角色对应多个账号，一个账号只有一个当前岗位 |
| 科室 - 系统用户 | 1:N | 医师归属科室；管理员/住院处可不归属临床科室 |
| 科室 - 病区 - 床位 | 1:N:N | 科室拥有多个病区，病区拥有多张床 |
| 患者 - 住院记录 | 1:N | 患者可多次住院，一次住院只属于一个患者 |
| 患者 - 患者过敏史 | 1:N | 过敏史是患者的可追溯历史记录 |
| 住院记录 - 转科记录 | 1:N | 一次住院可多次转科，转科不覆盖历史 |
| 住院记录 - 诊断/病历/护理评估/生命体征 | 1:N | 住院期间形成多条临床记录 |
| 住院记录 - 处方 - 处方明细 - 药品 | 1:N:N:1 | 一次住院多张处方，每张处方多种药品；一种药品可出现在多张处方 |
| 住院记录 - 检查检验医嘱 - 医嘱明细 - 项目 | 1:N:N:1 | 一次住院多次开立，每次可包含多个项目 |
| 检查检验医嘱明细 - 报告 - 结果明细 | 1:0..1:N | 未执行或未出报告时无报告；一份报告可有多项结果 |
| 住院记录 - 费用记录 | 1:N | 药品、检查、床位等产生多条费用 |
| 住院记录 - 押金流水 | 1:N | 多笔缴纳、退款或冲正流水 |
| 住院记录 - 出院结算 | 1:0..1 | 住院中没有结算，正式出院最多一条结算单 |
| 系统用户 - 临床/行政记录 | 1:N | 通过 `created_by`、`recorded_by`、`doctor_id`、`operator_id` 等字段记录责任人 |

## 5. 关键业务约束

1. 一个床位同一时刻只能被一条“住院中”的住院记录占用；办理转科或出院时在同一事务内释放旧床、占用新床。
2. 住院号、病案号、患者编号、处方号、检查医嘱号、押金收据号均唯一；身份证号可空但一旦录入不得重复。
3. 处方和检查检验医嘱必须关联住院记录和医师；已审核或已执行的医嘱不可直接删除，只能停止或作废并保留操作痕迹。
4. 报告结果至少填写定性值或定量值之一；异常标志可根据参考范围计算，也允许医师复核修改。
5. 出院结算前必须完成费用核对；结算成功后住院状态改为“已出院”，出院时间、结算人和床位释放动作同时提交。
6. 押金退款不得超过当前可退余额；退款、冲正和结算均需要住院处或最高管理员权限。
7. 医师只能维护本人负责或所在科室授权的临床记录；最高管理员可维护基础资料并查询全院数据；住院处可维护住院行政数据但不能修改医师病历正文。
8. 患者、住院记录和临床记录不做物理删除，使用状态或作废记录满足审计要求。

## 6. ER 图文字骨架

可据此在报告中绘制 Chen 或 Crow's Foot ER 图：

```text
角色 1 ─── N 系统用户 N ─── 1 科室 1 ─── N 病区 1 ─── N 床位
                                      │                    │
患者 1 ─── N 住院记录 N ─── 1 当前床位 ───────────────────┘
  │                  │
  └── N 患者过敏史   ├── N 转科记录 ─── 1 目标科室/目标床位
                     ├── N 诊断记录
                     ├── N 病历记录
                     ├── N 护理评估
                     ├── N 生命体征
                     ├── N 处方 ─── N 处方明细 ─── 1 药品
                     ├── N 检查检验医嘱 ─── N 医嘱明细 ─── 1 检查检验项目
                     │                                  │
                     │                                  └── 0..1 检查检验报告 ─── N 报告结果
                     ├── N 费用记录
                     ├── N 押金流水
                     └── 0..1 出院结算
```

其中“当前科室/当前床位”是住院记录的当前快照，“转科记录”保存完整变更历史；这两个层次同时存在是为了既能快速查询当前入住位置，又不丢失患者流转轨迹。

## 7. 课程设计落地建议

- 以上实体已超过课程要求的 5 张表，但仍保持 3NF：目录、主数据、交易、历史和结果分离。
- 演示主流程可选“入院登记 → 入院收押金 → 分配床位 → 医师诊断并开处方/检查 → 录入报告与护理评估 → 转科 → 出院结算”。
- 至少 3 个统计可直接基于这些表实现：各科室在院人数/床位使用率、各类疾病数量及住院天数、药品和检查费用构成（也可增加押金收支与出院人数趋势）。
- 种子数据应准备不少于 30 名患者及足够的住院、处方、医嘱、费用和体征关联记录；同一患者可有多条关联记录，以满足“关联关系数据不少于 200 条”的演示要求。

